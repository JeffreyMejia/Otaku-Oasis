/* exported data */
interface Entry {
  title: string;
  photoURL: string;
  episodes: string;
}

const entry: Entry = {
  title: 'string',
  photoURL: 'string',
  episodes: 'string',
};

renderSearch(entry);
